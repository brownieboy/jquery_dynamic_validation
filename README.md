jquery_dynamic_validation
=========================

Dynamic form validation tests with jQuery validate.  The validation rules are changed on-the-fly, depending on what options the user selects further up the form.
